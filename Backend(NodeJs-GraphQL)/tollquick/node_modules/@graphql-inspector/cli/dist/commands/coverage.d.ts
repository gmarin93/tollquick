import { SchemaCoverage } from '@graphql-inspector/core';
import { Renderer } from '../render';
export declare function outputJSON(coverage: SchemaCoverage): string;
export declare function coverage(documentsPointer: string, schemaPointer: string, options: {
    require: string[];
    write?: string;
    silent?: boolean;
    renderer?: Renderer;
    headers?: Record<string, string>;
}): Promise<void>;
