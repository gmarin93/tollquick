import { Renderer } from '../render';
export declare function similar(schemaPointer: string, name: string | undefined, threshold: number | undefined, options: {
    require?: string[];
    headers?: Record<string, string>;
    write?: string;
    renderer?: Renderer;
}): Promise<void>;
