import { Renderer } from '../render';
export declare function serve(schemaPointer: string, options: {
    renderer?: Renderer;
    require?: string[];
    headers?: Record<string, string>;
}): Promise<void>;
