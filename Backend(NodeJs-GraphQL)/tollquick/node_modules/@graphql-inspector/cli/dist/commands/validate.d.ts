import { Renderer } from '../render';
export declare function validate(documentsPointer: string, schemaPointer: string, options: {
    require?: string[];
    deprecated: boolean;
    noStrictFragments: boolean;
    maxDepth?: number;
    renderer?: Renderer;
    headers?: Record<string, string>;
}): Promise<void>;
