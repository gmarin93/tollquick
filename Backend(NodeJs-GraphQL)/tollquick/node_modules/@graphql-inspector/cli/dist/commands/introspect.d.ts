import { Renderer } from '../render';
export declare function introspect(schemaPointer: string, options: {
    token?: string;
    write?: string;
    require?: string[];
    renderer?: Renderer;
    headers?: Record<string, string>;
}): Promise<void>;
